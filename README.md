#1 These lines of code install the Chrome browser and the selenium and chromedriver-autoinstaller libraries. Selenium has been used to access a specific URL ([redacted link]): it waits for the page to render completely (in headless mode), and then extracts the full HTML content. The HTML content is saved to a file and assigned to the html_content variable for subsequent analysis.
#2 This block extracts lineage cell names from HTML content (obtained from a webpage) using the pandas library. It globally initializes the lists AB, MS, and E to store the cell names of their respective lineages. It performs a check for unclassified entries and prints the number and some examples of extracted cells before deduplication.
#3 Here, in the AB, MS, and E cell lists (populated by the previous block), duplicates are removed and cells sorted, verifying the unique cell counts against expected counts for each lineage (AB, MS, E); a verification report is printed. Finally, the unique cell lists are converted into pandas DataFrames (df_ab, df_ms, df_e).
________________________________________
#4 Detailed APO Data for AB Lineage. This Python cell contains a long multiline string (user_provided_detailed_apo_raw) representing a detailed list of user-provided APO (apoptotic) cells for the AB lineage. This list is intended to be the "authoritative" source for AB APO cells, used for building and verifying the AB lineage tree.
#5 Cleaning and Analyzing Detailed APO List. This Python block of code processes the user_provided_detailed_apo_raw string (from the previous cell). It cleans each line by removing leading and trailing whitespace and adds it to a user_detailed_apo_cells list. Finally, it prints the number of APO cells analyzed.
 
#6 This Python cell creates a unified DataFrame called df_all_lineages by concatenating the df_ab, df_ms, and df_e DataFrames. It adds a 'Lineage_Type' column to each DataFrame before concatenation and renames the cell name column to 'Cell_Name' for consistency. It displays the first 5 rows of the combined DataFrame.
#7 Downloading C. el- Genome (Chromosome V). The Biopython library is installed and a function download_and_load_chromosome_V programmatically downloads the compressed FASTA file of C. el. Chromosome V from Ensembl. It decompresses the file in memory and returns the clean DNA sequence. The sequence is stored in chromosome_v_sequence.
#8 This block defines a function ‘apply_telomeric_masking’ that dynamically identifies and masks telomeric blocks (TTAGGC and GCCTAA motifs repeated at least 3 times) in the DNA sequence. It replaces these motifs with 'N' to preserve coordinate linearity. It applies this function to chromosome_V_sequence and prints the number of masked bases. The resulting sequence is stored in chromosome_V_filtered_sequence.
#9 Scanning the Genome with a Sliding Window for (TRs) . This Python code defines a function ‘scan_genome_sliding_window’ that scans the masked genomic sequence using a sliding window approach. It extracts valid TRs (excluding 1, 2, and 3 bp monomers) and maps their spatial density. It returns a DataFrame (df_sliding_windows) with TR counts for each window.
#10 Generating the Global Tandem Repeats Profile. Here the program defines a function ‘generate_global_tr_profile’ that extracts individual TR details from the nested lists in the df_sliding_windows DataFrame and flattens them into a structured DataFrame (df_global_tr_profile). This DataFrame includes the absolute position, monomer sequence, monomer length, number of repetitions, and a complexity score. It prints the number of unique TRs recovered.
#11 For visualizing TR Spatial Density and Complexity, matplotlib and seaborn are used to generate a high-quality visualization of TR density and complexity distribution along the linear axis of Chromosome V. The plot is divided into two subplots: the first shows TR density per sliding window, while the second maps the complexity of individual TR loci.
#12 Exact Matching of TRs from E-lineage Subtrees: the function ‘find_exact_subtree_tr_match’ performs an exact mathematical match between detected TR parameters (monomer length and copy number) and two functional groups of E-lineage subtrees. It classifies TRs into functional categories.
#13 TR Collinearity Analysis: the function ‘analyze_tr_collinearity’ analyzes the spatial distribution of TR groups (Group 1 and Group 2 previously defined) across three macro-regions of Chromosome V. The goal is to test the HOX-like collinearity hypothesis. The function divides the chromosome into three zones (Anterior, Central, Posterior) and calculates the proportion and absolute counts of the different TR groups in each zone. It prints a summary and absolute counts. Finally, it performs this analysis on the df_exact_lineage_matches DataFrame.
#14 This Python cell defines the function ‘generate_ms_terminal_subtrees’ that models the 16 foundational subtrees of the MS lineage emerging after the 4 initial symmetric (trivial) division cycles. It maps their exact asymmetric depths (1, 2, or 3) and incorporates the 14 programmed apoptotic events (APO). The function creates a DataFrame (df_ms_terminal) that mathematically classifies the subtrees based on their asymmetric depth and death command (STOP or APO). It prints a summary of the classification, including tracked apoptotic nodes. Finally, it executes this function.
#15 Displaying First Elements of df_ms_lineage_matches. These simple Python lines use the display() function to show the first 3 rows of the df_ms_lineage_matches DataFrame. This DataFrame (which will be generated in the next cell) contains the results of the exact TR matching for the MS lineage, and this cell serves for a quick inspection of the data.
#16 Matrix-Based TR Matching for MS Lineage. This block defines the function ‘find_ms_matrix_tr_matches’ that performs matrix-based exact matching on genomic TRs for the MS lineage. It maps monomer length (L) to cell fate (STOP vs APO) and copy number (N) to asymmetric depth (1, 2, or 3 division cycles). The proposed theoretical model is that even monomer lengths favor structural/muscle fate (STOP), while odd monomer lengths or specific hexamers/heptamers trigger cell death cascades (APO). The function returns a DataFrame (df_ms_matches) with qualified TRs and their associated molecular commands. Finally, it executes this function on the global TR profile.
#17 MS Fate Collinearity Analysis. Here the function ‘analyze_ms_fate_collinearity’ analyzes the spatial and distribution of STOP (functional differentiation) vs APO (programmed cell death) fates across Chromosome V macro-regions. The goal is to test the collinearity of cell death in the MS lineage; it divides the chromosome into three zones and calculates the proportions and absolute counts of cellular fates per zone. Finally, it performs the analysis on the df_ms_lineage_matches DataFrame. Displaying First Elements of df_ms_fate_distribution: the first 3 rows of the df_ms_fate_distribution DataFrame are displayed; this DataFrame contains the results of the MS fate collinearity analysis (from the previous block) and this visualization allows for a quick inspection of the distribution of proportions and counts of cell fates.
#18 Analysis of Early Trivial Divisions Collinearity. 
This block defines the function ‘analyze_early_trivial_collinearity’ that analyzes the properties of TRs that coordinate early, phenotypically trivial (symmetric) division cycles compared to terminal asymmetric ones. It checks if early-generation counters occupy distinct genomic distributions. The function groups TRs based on their copy number (N) to infer their replication depth capacity, calculating the average monomer length, total number of loci found, and average genomic position for each copy number. Finally, it executes this analysis on TRs corresponding to the MS/E lineages.
#19 A few simple lines of code use the display() function to show the first 3 rows of the df_early_collinearity DataFrame. This DataFrame contains the results of the early trivial divisions collinearity analysis (from the previous cell) and serves for a quick inspection of the data.
#20 Defining the ‘draw_lineage_tree’ Function. Here it is defined the ‘draw_lineage_tree’ function which uses the graphviz library to graphically draw a cellular lineage tree. The function takes as input a dictionary of nodes (nodes_dict), the founder type (e.g., 'AB', 'MS', 'E'), a flag to show all nodes, and parameters for file saving. It assigns shapes, colors, and tooltips to nodes based on their state ('GO', 'STOP', 'APO') and draws edges between parents and children. It prints a confirmation message upon file saving.
#21 Building and analyzing the C. el. lineage trees for the AB, MS, and E founder cells: It defines functions to extract_lineage_info (to get suffix and delimiter), reconstruct_full_name (to build cell names), and get_parent_and_sister_lineage_suffixes (to determine parent and sister cells); build_lineage_tree Function: This is the core logic. It constructs a dictionary-based tree structure for each lineage. It takes lists of 'STOP' cells (terminal differentiation), 'APO' cells (programmed cell death), and the 'founder_type' (e.g., 'AB', 'MS', 'E'). It initializes the founder, identifies ancestors as 'GO' cells (still dividing), and assigns states based on a hierarchy (APO > STOP > GO). It then connects cells in a parent-child and sister relationship using a queue-based approach.
Tree Summaries: After building the trees, it calculates and prints summaries for each lineage (AB, MS, E), including the total number of STOP, GO, and APO cells, and verifies the total node count against an expected formula.
APO Cell Comparison: For the AB lineage, it explicitly compares the APO cells identified in the generated tree with a predefined 'authoritative' list (user_detailed_apo_cells) to ensure accuracy and report any discrepancies.
 
#22 #23 #24 #25, #26, #27 Generating and Displaying Tree Image.
#28 ‘Lineage Tree Building Logic’defines several helper functions for building and analyzing lineage trees:
-          ‘extract_lineage_info’ extracts the lineage suffix and delimiter from a full cell name.
-          ‘reconstruct_full_name’ reconstructs the full name of a cell from the founder type, lineage suffix, and delimiter.
-          ‘get_parent_and_sister_lineage_suffixes’ derives the lineage suffixes of the parent and sister.
-          ‘build_lineage_tree’ is the main function that constructs the tree structure (nodes, parents, children, 'GO', 'STOP', 'APO' states) from a list of terminal cells and an authoritative list of APO cells.
-          ‘count_cell_states’ counts the number of STOP, GO, and APO cells in a given tree.
-          ‘get_root_node’ returns the name of the tree's root node.
Subsequently, the block prepares the input data (normalizing AB cell names and explicitly adding some STOP cells) and then executes the build_lineage_tree function to create the AB, MS, and E lineage trees. Finally, it prints a detailed summary for each tree, including cell counts by state and a verification of the generated APO cells against the authoritative list.
 Performing Quality Checks for Lineage Trees: these lines of code define the function ‘perform_quality_checks’ that executes a series of quality checks on the lineage trees (AB, MS, and E) to ensure consistency with biological principles and expected structure:
   It verifies that APO and STOP cells do not have children and that GO cells have exactly two complementary (a/p, l/r, d/v) children.
   The function checks parent-child name consistency, ensuring that the parent's name is consistent with the child's and parent’s.
#29 Counting AB Cells with Short Names. This simple Python code iterates through the unique_AB_cells list and counts how many cells have a name whose length is less than 10 characters. It prints the final count.
#30 Classification of the 8 Founder Roots defines a dictionary subtree_data that models the behavioral profiles of the 8 founder roots of the intestinal lineage (E-lineage) after the 8E stage. Each entry includes the cell name, position, destination (intestinal rings), division profile (GO, asymmetric_GO, STOP), and the final number of cells produced. The dictionary is converted into a pandas DataFrame (df_subtrees). The cell then automatically classifies these subtrees by grouping them by division behavior (division_profile) and prints a detailed summary for each equivalent group, including behavioral profiles, final cell counts, and assigned target rings.
#31 TR Mapping and Filtering Function with Validation. These lines of code define the function ‘find_and_map_filtered_trs’ that searches for TRs in a DNA sequence. The main feature is the exclusion of monomeric (1bp), dimeric (2bp), and trimeric (3bp) monomer sequences that are considered "degenerate" within the repeated monomer itself (e.g., ATATAT is a degenerate dimer of AT). The function calculates the position, monomer, monomer length, and number of repetitions for each valid TR. It includes a validation test on a specially created test_sequence to demonstrate that degenerate 1, 2, and 3 bp TRs are correctly excluded, while valid TRs (like a hexamer or a tetramer) are detected.
